# ──────────────────────────────────────────────────────────────
#  Mon Amour Studio – Environment Variables Template
#  Copiar como .env y ajustar los valores
#  cp .env.template .env
# ──────────────────────────────────────────────────────────────

# ── General ───────────────────────────────────────────────────
COMPOSE_PROJECT_NAME=monamour

# ── Admin (email auto-promoted to admin role on register) ─────
ADMIN_EMAIL=user@email.com

# ── JWT ───────────────────────────────────────────────────────
JWT_SECRET=super-secret-key-change-in-production

# ── PostgreSQL (shared credentials for all service DBs) ──────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ── MinIO (Object Storage) ───────────────────────────────────
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# ── Kafka ─────────────────────────────────────────────────────
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# ── Redis ─────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── OpenTelemetry / Jaeger ───────────────────────────────────
OTLP_ENDPOINT=http://jaeger:4317

# ── Grafana ──────────────────────────────────────────────────
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=admin

# ── Traefik – Domain & TLS ───────────────────────────────────
DOMAIN=monamourstudio.local
ACME_EMAIL=user@email.com

# ── Traefik – Let's Encrypt DNS-01 Challenge ─────────────────
# Proveedor DNS: Cloudflare (cambiar si se usa otro)
# Ver: https://doc.traefik.io/traefik/https/acme/#providers
CF_DNS_API_TOKEN=your-cloudflare-api-token-here

# Otros proveedores comunes (descomentar el necesario):
# ROUTE53_ACCESS_KEY_ID=
# ROUTE53_SECRET_ACCESS_KEY=
# DO_AUTH_TOKEN=
# GCE_PROJECT=
# GCE_SERVICE_ACCOUNT_FILE=
